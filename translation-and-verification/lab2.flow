import runtime;
import string;

main() {
	integers: [int] = [1, 2, 3, 4];
//  str: [string] =  fold(integers, [], \x, y -> arrayPush(x, i2s(y)));
  sum: int = fold(integers, 0, \x, y-> x+y); 

  println(i2s(sum));
}
